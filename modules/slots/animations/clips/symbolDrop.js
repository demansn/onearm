import gsap from "gsap";

/**
 * @description Plays drop/fall-in animation on a reel symbol
 * @param {import('../../reels/ReelSymbol.js').ReelSymbol} symbol
 * @param {Object} [options]
 * @param {string} [options.animation="in"]
 * @param {number} [options.timeScale=1.4]
 * @returns {gsap.core.Timeline}
 */
export function symbolDrop(symbol, { animation = "in", timeScale = 1.4 } = {}) {
    return null;
}
