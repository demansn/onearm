/**
 * @fileoverview Type re-exports for extractors.
 * Extractors work with AbstractNode from the adapter layer.
 */

export type { AbstractNode, UnifiedPaint as Paint, UnifiedColor as RGB } from '../adapters/types';
export type { UnifiedColor as RGBA } from '../adapters/types';
